/** @type {import('next').NextConfig} */
const nextConfig = {
  // No experimental appDir needed for Next.js 14+
  // No env config needed since we're using hardcoded values
}

module.exports = nextConfig